Gittin started with git

git init Primer comando para que git empiece a trackear

git add sube el archivo al staging area

git commit confirma todos los cambios del stagging area

git status da reporte

git add . (todo)

git add "*.txt" (todo los txt con regex)

git log (hitoria de todos los commits)


.gitignore (archivo donde se mencionan los files que no queremos que se suban a git) github/gitignore


git remote add origin https://github.com/sfrittaoni/gittingstarted.git (primer comando para agregar un repo en github)

git branch -M main (crea la rama)
git push -u origin main (-u es default)

(github pass: token)

secuencia:
git add .
git commit -m 'mensaje'
git push (no hace falta especificar porque lo hicimos default)


